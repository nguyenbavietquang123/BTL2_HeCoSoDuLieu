// Dummy data for illustration; replace with real data or API calls

// Calculate averages

// Populate the table


// Toggle visibility
document.getElementById("showAverageButton").addEventListener("click", () => {
    const container = document.getElementById("averageSalaries");
    if (container.style.display === "none") {
        container.style.display = "block";
    } else {
        container.style.display = "none";
    }
});
